package baseball.controller;

import baseball.service.BallsGenerator;

public class BaseballGame {
    public static void start(BallsGenerator ballsGenerator) {
        /*computer = new Computer(generateComputerNumber());
        Player player = new Player(PLAY);
        initPlayerNumbers();
        while (player.isPlaying()) {
            playingGame();
            askReplay(player);
            reStartGame(player);
        }*/
    }
}
